apiVersion: v1
kind: Service
metadata:
  name: {{ include "matrix-media-chart.fullname" . }}
  labels:
    {{- include "matrix-media-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "matrix-media-chart.selectorLabels" . | nindent 4 }}
